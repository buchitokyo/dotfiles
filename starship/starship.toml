# エディターの補完を設定スキーマに合わせて取得
"$schema" = 'https://starship.rs/config-schema.json'

# シェルのプロンプトの間に空行を挿入する
add_newline = true

# ref https://github.com/starship/starship/discussions/1107?sort=top#discussioncomment-10214050
format = """[╭ π ](bold green)$username$directory$all$battery$line_break$character"""

[character]
success_symbol = '[╰─>](bold green)'
error_symbol = '[╰─>](bold green)'

[status]
disabled = false
style = "red"
symbol = " \\(╯°□°）╯︵ ┻━┻ "
format = "\b[$symbol$status](fg:$style)"

[git_branch]
format = '[$symbol$branch(:$remote_branch) ]($style)'

[git_commit]
tag_disabled = false
tag_symbol = "🏷 "

# ref https://github.com/starship/starship/discussions/1107#discussioncomment-345147
[git_status]
style = "#f05133"
format = "($conflicted$staged$modified$renamed$deleted$untracked$stashed$ahead_behind\b )"
conflicted = "[ ](fg:88)[   ${count} ](fg:white bg:88)[ ](fg:88)"
staged = "[M$count ](fg:green)"
modified = "[M${count} ](fg:red)"
renamed = "[R${count} ](fg:208)"
deleted = "[ ${count} ](fg:208)"
untracked = "[?${count} ](fg:red)"
up_to_date = '[✓](bold green)'
stashed = "[📦${count} ](fg:cyan)"
ahead = "[ ${count} ](fg:purple)"
behind = "[ ${count} ](fg:yellow)"
diverged = "[](fg:88)[⚡️](fg:white bg:88)[ ${ahead_count} ](fg:purple bg:88)[ ${behind_count} ](fg:yellow bg:88)[ ](fg:88)"

[[battery.display]]
threshold = 100
style = "bold blue"

[battery]
full_symbol = "🤗"
charging_symbol = "😌"
discharging_symbol = "😨"

[time]
disabled = false
format = '⏳ [$time]($style) '
time_format = '%m/%d %R'

# [memory_usage]
# disabled = false
# threshold = -1
# style = "bold dimmed green"

[aws]
disabled = true

[gcloud]
disabled = true

[bun]
disabled = true

[python]
disabled = true
